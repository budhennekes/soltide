# Lessons

- **2026-07-02 — "Slightly overwhelming" feedback on v1.** Bud wants suggested times spelled out ("step outside 6:37–7:07 AM"), not just state labels, and fewer elements on screen. Rule: every recommendation should include a concrete time; when adding anything to the Today screen, remove or shrink something else. Keep timeline to 3 rows max.
- **2026-07-02 — "Still a bit crowded" after round 2.** Trimming copy wasn't enough; the structure was the problem (two big cards competing). Fix that worked: merge the overview (arc) into the hero, shrink the action to a pill, cap every explanation at one sentence, delete anything that repeats. Rule: one hero card owns the screen; actions are compact, never full-width banners; each idea appears exactly once per screen.
- **Preview servers can't read ~/Documents** (sandboxed → 404). Serve from a synced scratchpad copy; source of truth stays in `circadian-app/index.html`.
- Test any state with `?sim=HH:MM` or `?sim=2026-12-21T12:00` in the URL.
